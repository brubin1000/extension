{
  "name": "atms-extension",
  "version": "1.0.0",
  "description": "ATMS - Artist Tour Management System Chrome Extension",
  "type": "module",
  "scripts": {
    "build": "esbuild background/service-worker.js --bundle --outfile=dist/service-worker.js --format=esm --target=es2020",
    "watch": "esbuild background/service-worker.js --bundle --outfile=dist/service-worker.js --format=esm --target=es2020 --watch",
    "test": "echo \"No tests yet\" && exit 0"
  },
  "dependencies": {
    "realm-web": "^2.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  },
  "author": "ATMS",
  "license": "MIT"
}
